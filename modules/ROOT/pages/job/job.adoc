= CAM Job
:page-origin: https://wiki.freecad.org/CAM_Job
:page-origin-revision: 375e2bddc0 2026-07-08
:page-menu: CAM → New Job
:page-shortcut: P J
:page-imported: true

[.guicommand]
Menu location:: menu:CAM[New Job]
Workbenches:: xref:index.adoc[CAM]
Default shortcut:: kbd:[kbd:[P] kbd:[J]]
See also:: xref:output/post-process.adoc[CAM Post], xref:output/postprocessor-customization.adoc[CAM Postprocessor Customization]

== Description

The image:CAM_Job.svg[width=24] xref:job/job.adoc[CAM Job] command creates a new Job object in the active document. The Job object contains the following information:

. A list of Tool-Controller definitions, specifying the geometry, Feeds, and Speeds for the Path Operations Tools.
. A Workflow sequential list of Path Operations.
. A Base Body—a clone used for offset.
. A Stock, representing the raw material that will be milled to CAM Workbench.
. A SetupSheet, containing inputs used by the Path Operations, including static values and formulas.
. Configuration parameters specifying the output G-Code job's destination path, file name, and extension, and the xref:output/post-process.adoc[postprocessor] (used to generate the appropriate dialect for the target CNC Controller, and customize Units, Tool Changes, Parking, etc.).

== Usage

. There are several ways to invoke the command:
* Press the image:CAM_Job.svg[width=16] xref:job/job.adoc[New Job] button.
* Select the menu:CAM[New Job] option from the menu.
* Use the keyboard shortcut: kbd:[P] then kbd:[J].

The Job GUI dialog box has five horizontally aligned tabs: menu:General[], menu:Output[], menu:Setup[], menu:Tools[], and menu:Workplan[]. The user can at any time utilize the btn:[OK] or btn:[Cancel] options within the dialog.

== General

image:Job_1.jpg[]

* *Label*: The label of the Job as displayed in the Tree View.
* *Model*: The Base Object which defines by its shape the paths of the job. If it is a Part Design object, it is usually the Body you select here. If you have an element selected in the tree _before_ you click the "Add Job" icon that element is already entered here. You can change it by selecting a different element from the dropdown menu.
* *Description*: You can add some notes to the job here. Notes are only for your information and have no effect on the path.

== Output

image:Job_2.jpg[]

* *Output File*: Set the name, extension, and the file path of the G-Code output. You can use the following placeholders:
** *%D* directory of the active document
** *%d* name of the active document (without extension)
** *%M* user macro directory
** *%j* name of the job

* *Processor*: Select the xref:output/post-process.adoc[postprocessor] for your machine.
* *Arguments*: Add arguments for the xref:output/post-process.adoc[postprocessor] as needed.
* *Machine*: Optionally select a machine configuration. When a machine is selected, it defines the postprocessor and related output settings for the job.

== Setup

image:Job_3.jpg[]

* *Stock*: set the size and shape of the raw material.
* *Orientation*: Selected Edge or Face is used to orient Base or Stock accordingly.
* *Alignment*: select a Vertex to set origin or move Base or Stock

== Tools

image:Job_4.jpg[]

Add the tool(s) from your xref:tools/library-manager.adoc[Tooltable] that you need for the operations at this job.

After adding a tool, you can set/change the feedrate and spindle speed if you need a different feedrate in this job. A change here doesn't change the parameters stored in the tooltable.

You can delete the default tool if you have your own tool added.

== Workplan

image:Job_5.jpg[]

If you have a job that contains more than one path operation, you can determine in which order the operations should be done. To reorder, select an operation and push the up or down button.
